/**
 * Placeholder editor denah — implementasi detail di phase berikutnya.
 * Mode edit: drag kamar, resize, tambah koridor/tangga/lift.
 */
export default function FloorPlanEditor() {
  return (
    <div className="rounded-xl border border-dashed border-gray-300 bg-gray-50 p-6 text-center text-sm text-gray-500">
      Floor Plan Editor — segera hadir (manager/owner only)
    </div>
  );
}
